-- cabal.project  –  PFL (SimCLR NIDS)

packages: .

-- CRITICAL for Linux: libtorch-ffi's rpath is not propagated on Linux,
-- so hasktorch must use shared linking.
shared: True
executable-dynamic: True

-- Allow building with newer versions
allow-newer:
  libtorch-ffi:base
  , hasktorch:base
